/**
 * @author ankostyuk
 */

var chai        = require('chai'),
    assert      = chai.assert,
    expect      = chai.expect,
    should      = chai.should();

describe('', function(){
    it('', function(done){
        done();
    })
})
